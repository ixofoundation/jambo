## What changed

<!-- One or two sentences. What does this PR do, and why? -->

## Checklist

- [ ] `yarn verify` passes locally
- [ ] `constants/config.json` still validates (run `yarn validate:config` if this PR touches it)
- [ ] Generated files are current — `yarn gen` produces no diff
- [ ] New or changed step ids have an entry in `constants/stepCatalogue.ts` and an e2e driver
- [ ] Docs updated if the fork/customization surface changed (`AGENTS.md`, `README.md`)

## Verification

<!-- How did you check this works? Paste command output, or say which flow you exercised. -->
